{% extends "../layout.twig" %}

{% block content %}
    <div class="jumbotron text-center">
        Página Inicial
    </div>

    {% for el in data %}
        <p>{{ el.title }}</p>
    {% endfor %}
{% endblock %}